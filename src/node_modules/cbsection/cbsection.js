var $ = require('jquery');

function CBSection(dataobject){

	dataobject = typeof dataobject !== 'undefined' ? dataobject : {};
	this.sections = typeof dataobject.sections !== 'undefined' ? dataobject.sections : ][;
}

CBSection.prototype.editorView = function editorView() {
	var aux = $(window.document.createElement('div'));
	aux.css('left', this.position[0]);
	aux.css('top', this.position[1]);
	aux.addClass('draggable');
	aux.css('position','relative');
	return aux;
};

CBSection.prototype.save = function save() {
	return {'type':this.type,'position':this.position};
}
module.exports = CBSection;